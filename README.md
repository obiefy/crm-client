# CRM-CLIENT

Client demo FOR CRM API

# Installation:

- Clone Repo:

  `git clone git@github.com:obiefy/crm-client.git`

- Change Dir:

  `cd crm-client`

- Install dependencies:

  `npm install`

- Run application:

  `npm start`
